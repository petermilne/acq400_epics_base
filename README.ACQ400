echo ACQ400 branched from BASE 7.0.2 and cross compiled

(cd ..; sudo ln -s ${PWD} /usr/local/epics)
sudo ln -s /home/pgm/PROJECTS/EPICS7 /usr/local/epics

cd base; make
(cd ../modules; for dir in *;do (cd $dir; make); done
./harvest

